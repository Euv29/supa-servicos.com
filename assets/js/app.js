// // Seleciona as imagens
// const images = document.querySelectorAll('.service-img img');
// let currentIndex = 0;

// // Função para alternar as imagens
// function changeImage() {
//     // Oculta a imagem atual
//     images[currentIndex].style.opacity = '0';
    
//     // Atualiza o índice para a próxima imagem
//     currentIndex = (currentIndex + 1) % images.length;
    
//     // Mostra a próxima imagem
//     images[currentIndex].style.opacity = '1';
// }

// // Define um intervalo para chamar a função de mudança de imagem a cada 3 segundos (3000 milissegundos)
// setInterval(changeImage, 2000);
